name: Actions Trigger

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - production

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Print environment
        run: echo "Running in ${{ github.event.inputs.environment }} environment"

      - name: Use ACCESS_TOKEN
        env:
          ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
        run: echo "ACCESS_TOKEN is set"

      - name: Compile Java project
        run: mvn compile

      - name: Copy dependencies
        run: mvn dependency:copy-dependencies

      - name: Run CompaniesTest
        run: java -cp "target/classes:target/dependency/*" com.CompaniesTest
